{
  "mcpServers": {
    "parallel-dev-mcp": {
      "command": "parallel-dev-mcp",
      "cwd": "/Users/flink/parallel-dev-mcp",
      "env": {
        "HOOKS_MCP_CONFIG": "${PWD}/examples/hooks",
        "DANGEROUSLY_SKIP_PERMISSIONS": "true",
        "PROJECT_ROOT": "${PWD}",
        "HOOKS_CONFIG_DIR": "${PWD}/config/hooks"
      }
    }
  }
}